@model Cover
@using Kendo.Mvc.UI;
@using SpadCompanyPanel.Infrastructure.Helpers;
@using SpadCompanyPanel.Core.Models;
@{
    ViewBag.Title = "کاور سایت";
}
<input type="hidden" id="nav_active" value="article_control">
<input type="hidden" id="nav_item_active" value="articles">
<div class="card card-custom">
    @*<div class="card-header flex-wrap border-0 pt-6 pb-0">
        <div class="card-title">
            <h3 class="card-label">
                @ViewBag.Title
                <span class="d-block text-muted pt-2 font-size-sm">ویرایش کاور سایت</span>
            </h3>

        </div>
        <div class="card-toolbar">
            <a href="/Admin/Articles" class="btn btn-light-success" style="margin-left:20px">
                <i class="flaticon2-arrow"></i> بازگشت
            </a>
        </div>
    </div>*@
  
</div>
<div class="col-lg-6">
    <div class="card card-custom gutter-b example example-compact">
        <div class="card-header">
            <h3 class="card-title">
                ویرایش کاور سایت
            </h3>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.Id)
            <div class="card-body">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model => model.Title, new { @class = "control-label col-md-2" })
                    @Html.EditorFor(model => model.Title, new { htmlAttributes = new { @class = "form-control " } })
                    @Html.ValidationMessageFor(model => model.Title, "", new { @class = "form-text text-danger" })
                </div>
                <div class="form-group">
                    @Html.LabelFor(model => model.SubTitle, new { })
                    @Html.TextAreaFor(m => m.SubTitle, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.SubTitle, "", new { @class = "form-text text-danger" })
                </div>
            </div>
            <div class="card-footer">
                @*<a href="@Url.Action("Index")" class="btn btn-secondary">انصراف</a>*@
                <input type="submit" value="ثبت" class="btn btn-primary mr-2" />
            </div>
        }
    </div>
</div>

<!-- Modal -->
@*<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header" id="myModalHeader">
                <h5 class="modal-title" id="myModalLabel">Modal Title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <i aria-hidden="true" class="ki ki-close"></i>
                </button>
            </div>
            <div class="modal-body" id="myModalBody">
                ...
            </div>
        </div>
    </div>
</div>*@
<!-- Modal -->
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>

        $(document).ready(function () {
            $('.k-i-filter').removeClass("k-i-filter").addClass("k-icon k-i-more-vertical");
        });
    </script>
    <script>
        function btnDelete(id) {
            $.get("/Admin/ArticleHeadLines/Delete/" + id, function (result) {
                $("#myModal").modal();
                $("#myModalLabel").html("حذف سر تیتر");
                $("#myModalHeader");
                $("#myModalBody").html(result);
            });
        }
    </script>
}